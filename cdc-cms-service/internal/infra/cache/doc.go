// Package cache hosts Redis-backed adapters (snapshot cache, idempotency
// locks, rate limits, alert dedup, lockout counters).
//
// Phase 2 v2 / P1 — placeholder. The existing `pkgs/rediscache` will be
// wrapped behind ports interfaces in later phases.
package cache
